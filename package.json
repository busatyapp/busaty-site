{
  "name": "busaty-site",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "node ./node_modules/vite/bin/vite.js build && node scripts/postbuild.js && node scripts/sitemap.js"
  },
  "dependencies": {
    "vite": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
